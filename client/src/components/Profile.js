import React, { Fragment } from 'react';

const Profile = () => {
    return (
        <Fragment>
            <h1>Profile</h1>
            [Personal info]
            [Logout button]
        </Fragment>
    );
};

export default Profile;