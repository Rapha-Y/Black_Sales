import React, { Fragment } from 'react';

const Header = () => {
    return (
        <Fragment>
            <h2>--Header</h2>
            [Logo] / [Button A] [Button B]
        </Fragment>
    );
};

export default Header;